export interface ProviderRecord {
  code: number;
  name: string;
  phone: string;
  address: string;
  contact: string;
  ruc: string;
}

export interface PaymentOption {
  code: number;
  description: string;
}
